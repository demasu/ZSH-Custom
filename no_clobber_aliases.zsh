#-------------------------------------------------------------
# Aliases to prevent accidentally clobbering files
#-------------------------------------------------------------
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'
alias mkdir='mkdir -p'